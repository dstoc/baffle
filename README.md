# baffle

Baffle is a standalone daemon for creating lightweight, policy-controlled HTTP/HTTPS proxies on demand. It provides domain and path allowlisting, selective HTTPS interception and secure credential injection. Each proxy has its own configuration, Unix socket and lifecycle, while all proxies share one daemon process.
