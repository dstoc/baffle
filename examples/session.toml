version = 1
operation = "create"

[session]
persistent = false
# Optional stable path relative to daemon.socket_dir:
# socket_name = "cladding/github.sock"

[[rules]]
host = "example.com"
mode = "tunnel"
ports = [443]
